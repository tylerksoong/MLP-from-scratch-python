# MLP-from-scratch-python
This is a simple mlp built from scratch only using NumPy, trained the mnist data set, it's a next js project, so run app.py to bootup the backend and then do npm run dev on in the mlp demo folder to bootup the front end.

The mlp in the website is current_weights, but you can train it again by running train.py.
